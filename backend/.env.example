# Server
PORT=5000
NODE_ENV=production

# Frontend URL (for CORS)
FRONTEND_URL=https://titleiq.titleslice.com

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-secret-jwt-key-change-this

# Encryption Secret for API keys (generate with: openssl rand -base64 32)
ENCRYPTION_SECRET=your-secret-encryption-key-change-this

# Groq API Key (free tier)
# Get yours at: https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key

# Email Configuration (for password reset)
# Supported providers: resend, sendgrid
MAIL_PROVIDER=resend
MAIL_FROM="TitleIQ <no-reply@tightslice.com>"

# Resend API Key (recommended - https://resend.com/api-keys)
RESEND_API_KEY=your-resend-api-key

# SendGrid API Key (alternative - https://app.sendgrid.com/settings/api_keys)
# SENDGRID_API_KEY=your-sendgrid-api-key
